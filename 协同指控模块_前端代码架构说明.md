# 协同指控模块：前端代码架构说明（任务理解 / 资源清单）

> 更新时间：2026-04-23  
> 适用范围：Mission Control -> 协同指控模块（coordination）

## 1. 目标

本文档用于说明当前协同指控模块的前端组件分层、源码位置，以及后续新增面板（如资源清单）的推荐扩展方式，便于持续迭代和回溯。

---

## 2. 当前源码位置总览

### 2.1 模块入口与挂载

- 模块清单（manifest）  
  `src/features/mission-control/modules/coordination/manifest.js`
- 模块右侧主入口（右侧壳层，负责子面板切换）  
  `src/features/mission-control/modules/coordination/panels/CoordinationRightPanel.vue`
- 模块左侧边栏（“简报”页签内容）  
  `src/features/mission-control/modules/coordination/panels/CoordinationLeftPanel.vue`

### 2.2 右侧子面板（组件化拆分后）

- 任务理解子面板（命令列表 + 命令概要/详情 + 标准化结果）  
  `src/features/mission-control/modules/coordination/panels/right/TaskUnderstandingPanel.vue`
- 资源清单子面板（资源列表 + 资源详情 + 点位表）  
  `src/features/mission-control/modules/coordination/panels/right/ResourceCatalogPanel.vue`

### 2.3 状态与数据层

- 任务理解状态与行为（解析、关联、删除、编辑等）  
  `src/features/mission-control/modules/coordination/state/useTaskUnderstandingState.js`
- 协同模块数据模型（DTO、mock 数据、URL 常量、请求响应构造）  
  `src/features/mission-control/modules/coordination/data/commandDataModel.js`

### 2.4 模块级协同状态（壳层）

- 子视图枚举、当前子视图、模块切换协同逻辑  
  `src/features/mission-control/shell/useMissionControlDemoShell.js`

---

## 3. 当前组件分层设计

### 3.1 壳层职责（CoordinationRightPanel）

只负责：
1. 读取 `activeSubviewId`
2. 按子视图切换渲染对应子组件
3. 兜底显示占位（未接入子视图）

不再承担具体业务渲染和复杂状态编排。

### 3.2 业务子面板职责

- `TaskUnderstandingPanel`：任务理解业务 UI、交互、语义标识
- `ResourceCatalogPanel`：资源清单业务 UI、资源详情展示、点位结构展示

### 3.3 状态层职责（useTaskUnderstandingState）

统一管理：
- 命令选中
- 任务理解触发流程
- 关联/删除动作
- 任务编辑草稿
- 结果视图切换与展开收起状态

组件只消费状态和动作，不直接散落业务流程。

---

## 4. 新增“资源清单”后的推荐扩展规范

后续新增同级页面（例如“行动序列”“方案规划”）建议复用该模式：

1. 在 `panels/right/` 新建独立子面板组件  
2. 在 `state/` 新建独立 hook（如 `useActionSequenceState.js`）  
3. 在 `CoordinationRightPanel.vue` 中做子面板路由映射  
4. 所有结构化字段与接口模型优先落在 `data/` 层  
5. 所有可跳转/可操作关键节点补齐语义 target/action 标识

---

## 5. 建议的下一步重构（可选）

当前 `useTaskUnderstandingState` 仍是任务理解专用状态。若后续多个子面板存在共享选择状态，建议新增：

- `state/useCoordinationWorkspaceState.js`

用于承接跨子面板共享状态（例如当前命令、当前资源、统一筛选条件、缓存索引等），并由各子面板的 state hook 组合使用，减少重复请求和状态不一致问题。

---

## 6. 文档联动关系

- 接口/数据结构：`协同指控模块_接口与数据结构总结.md`
- 后端联调精简说明：`协同指控模块_后端联调精简版.md`
- 本文档：负责前端组件架构与扩展策略


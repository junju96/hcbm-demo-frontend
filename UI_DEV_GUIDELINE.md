# UI_DEV_GUIDELINE

本规范用于约束协同指控模块后续界面开发，确保风格统一、交互一致、可回溯。

## 1. 视觉基线

1. 以“示例模块”风格为主（深色底 + 青绿描边 + 清晰信息层级）。
2. 允许局部亮色强调重点状态，但不得破坏整体色彩一致性。
3. 标题和关键文案不得过小、过暗；优先保证可读性。

## 2. 布局与容器规则

1. 任务理解页固定结构：左命令列表、右上详情、右下结果。
2. 外层容器必须随内容自适应增高，始终包裹内部控件。
3. 禁止依赖不必要的固定高度截断信息（除非明确是滚动区域设计）。
4. 编辑态与展示态布局保持一致，避免切换跳动。

## 3. 文本层级规范

1. 标题 > 小节标题 > 字段值 > 字段标签，层级清晰。
2. 字段标签与字段值必须可区分（字号、色彩、字重至少两项不同）。
3. 段落文本建议保留首行缩进或一致的左对齐规则，保证扫描性。

## 4. 状态与交互规范

1. 命令状态由交互驱动，不使用静态占位状态：
   - 未执行任务理解：`待理解`
   - 已执行任务理解：`已处理`
2. 结果区必须提供 `任务列表 / 资源列表` 切换，且选中态明显可辨。
3. 结果区必须提供“展开/收起详细条目”开关。
4. 请求信息文案统一中文化：`请求序号`。

## 5. 任务条目编辑规范

1. 每个任务项提供 `修改` 入口。
2. 编辑态必须提供 `保存 / 取消`。
3. 可编辑字段（当前版本）：
   - 任务名称
   - 内容
   - 任务区域
   - 开始时间
   - 结束时间
4. 编辑态高亮需与主色系一致（优先青绿），避免突兀暖色。

## 6. 资源条目展示规范

1. 资源条目需展示基础字段：资源类型、区域属性、坐标点数。
2. 需展示每个点位坐标明细（点位、纬度、经度、高度）。
3. 坐标点数必须与 `location.length` 一致，不可与数据源脱节。

## 7. 依赖字段文案规范

1. 统一格式：`命令1，任务2，资源1`。
2. 禁止出现旧格式：`任务:1 / 资源:1`、`命令:1`。
3. 如有新增依赖类型（方案、临机方案），按同样规则输出：`方案3`、`临机方案2`。

## 8. 文档维护规范

1. 接口或数据结构变更，必须同步更新：
   - `协同指控模块_接口与数据结构总结.md`
   - `协同指控模块_后端联调精简版.md`
2. 每日工作结束，更新当日工作日志并维护“功能维度索引”。

## 9. 提交规范

1. 提交信息必须包含：
   - 标题（type: subject）
   - 具体变更清单（多条 bullet）
2. 涉及界面改动提交前必须通过构建验证：`npm run build`。

## 10. 开发自检清单（每次 PR/提交前）

1. 布局是否稳定（无截断、无跳动）。
2. 标题与正文是否足够清晰（字号/颜色）。
3. 状态是否由真实交互驱动（非静态写死）。
4. 任务与资源展示结构是否统一。
5. 依赖文案格式是否符合规范。
6. 数据展示是否与数据源一致（特别是点位数量）。
7. 构建是否通过，文档是否同步更新。


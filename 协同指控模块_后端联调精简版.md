# 协同指控模块后端联调精简版（1页）

## 1. 接口清单

### 1.1 任务理解
- Method: `POST`
- URL: `/commandAndControl/command_decompose`
- 用途: 根据命令文本解析任务列表与资源列表

请求体（核心字段）
```json
{
  "RequestType": "DECOMPOSE",
  "RequestID": "87654321",
  "RequestData": {
    "CommandID": "CMD-20260401-001"
  }
}
```

响应体（核心字段）
```json
{
  "code": 200,
  "responseID": "87654321",
  "data": {
    "commandId": "CMD-20260401-001",
    "Analys_results": {
      "mission_info": [1, 2, 3],
      "resource_info": [1, 2]
    }
  }
}
```

### 1.2 关联/删除
- Method: `POST`
- URL: `/commandAndControl/command_update`
- 用途: 对命令执行 `associate` 或 `delete`

请求体（核心字段）
```json
{
  "RequestType": "UPDATE",
  "RequestID": "876543210",
  "RequestData": {
    "UpdateDetail": {
      "operation": "associate",
      "master_type": "command",
      "command_id": [1],
      "mission_id": [1, 2, 3],
      "resource_id": [1, 2]
    }
  }
}
```

响应体（核心字段）
```json
{
  "code": 200,
  "responseID": "876543210",
  "data": {
    "operation": "associate",
    "result": "success"
  }
}
```

## 2. 主要数据结构

### 2.1 任务结构（mission）
```json
{
  "mission_id": 1,
  "mission_name": "区域侦察命令-机动任务",
  "mission_type": "MANEUVER_TASK",
  "mission_detail": {
    "content": "...",
    "description": "...",
    "target": "区域B",
    "time": "2026-03-30 18:00:00",
    "duration": "待定"
  },
  "dependencies": {
    "commands": [1],
    "missions": [],
    "plans": [],
    "instant_plans": [],
    "resources": []
  },
  "connections": {
    "connected_commands": [1],
    "connected_plans": [],
    "connected_instant_plans": [],
    "connected_resources": [2]
  }
}
```

### 2.2 资源结构（resource）
```json
{
  "resource_id": 1,
  "resource_name": "区域A",
  "resource_type": "REGION",
  "resource_detail": {
    "type": "neutral",
    "location": [
      {"point": "region_point_1", "latitude": "115.704931", "longitude": "40.281571", "altitude": "2.123"}
    ]
  }
}
```

## 3. 前端调用链（联调重点）

1. 用户点击“任务理解”按钮
2. 前端调用 `POST /commandAndControl/command_decompose`
3. 后端返回 `mission_info/resource_info`
4. 前端按 ID 映射并渲染任务列表、资源列表、依赖关系

1. 用户点击“关联/删除”按钮
2. 前端调用 `POST /commandAndControl/command_update`
3. 后端返回 `operation + result`
4. 前端展示操作结果提示

## 4. 代码定位

- 接口与数据模型定义: `src/features/mission-control/modules/coordination/data/commandDataModel.js`
- 界面调用入口: `src/features/mission-control/modules/coordination/panels/CoordinationRightPanel.vue`


# 协同指控模块：接口与数据结构总结

本文档用于说明本次“任务理解”相关接口定义与数据结构定义的代码位置，以及在界面中的使用情况。  
> 注：2026-04-23 起，右侧面板已完成组件化拆分，任务理解主界面迁移到 `TaskUnderstandingPanel.vue`。  
> 注：2026-04-24 起，任务理解已接入前端本地存储，资源清单改为按本地数据库中的已解析资源集合展示。

## 1. 接口定义位置

接口常量与请求/响应构造函数统一定义在：
- `src/features/mission-control/modules/coordination/data/commandDataModel.js`

### 1.1 RESTful URL 定义
- `COORDINATION_API_URLS`（decompose / update）
- 位置：`commandDataModel.js:3`
- 当前值：
  - `decompose: /commandAndControl/command_decompose`
  - `update: /commandAndControl/command_update`

### 1.2 任务理解（DECOMPOSE）
- 请求体构造：`buildDecomposeRequest`
- 位置：`commandDataModel.js:274`
- 响应体构造：`buildDecomposeResponse`
- 位置：`commandDataModel.js:282`

### 1.3 关联/删除（UPDATE）
- 请求体构造：`buildUpdateRequest`
- 位置：`commandDataModel.js:294`
- 响应体构造：`buildUpdateResponse`
- 位置：`commandDataModel.js:314`

## 2. 数据结构定义位置

### 2.1 结构体（JSDoc Typedef）
定义均位于：`commandDataModel.js`

- `DependencyRefs`：`commandDataModel.js:9`
- `MissionDetail`：`commandDataModel.js:19`
- `MissionRecord`（包含 `dependencies` 字段）：`commandDataModel.js:29`
- `DecomposeRequest`：`commandDataModel.js:45`
- `UpdateRequest`：`commandDataModel.js:53`
- `DecomposeResponse`：`commandDataModel.js:69`
- `UpdateResponse`：`commandDataModel.js:83`

### 2.2 Mock 主数据
- 命令数据：`commandRecords`
- 任务数据：`missionRecords`
  - `dependencies` 字段示例位置：`commandDataModel.js:166`, `:191`, `:216`
- 资源数据：`resourceRecords`

### 2.3 按命令生成分析结果
- `createMockAnalysisByCommand`
- 位置：`commandDataModel.js:328`
- 该函数会把 URL、请求体、响应体、任务列表、资源列表拼装成前端直接可消费结构。

## 3. 界面使用位置（组件化后）

核心界面文件：
- 右侧壳层：`src/features/mission-control/modules/coordination/panels/CoordinationRightPanel.vue`
- 任务理解子面板：`src/features/mission-control/modules/coordination/panels/right/TaskUnderstandingPanel.vue`
- 资源清单子面板：`src/features/mission-control/modules/coordination/panels/right/ResourceCatalogPanel.vue`
- 任务理解状态：`src/features/mission-control/modules/coordination/state/useTaskUnderstandingState.js`
- 本地存储封装：`src/features/mission-control/modules/coordination/state/taskUnderstandingLocalDb.js`

### 3.1 导入与初始化
- `TaskUnderstandingPanel.vue` 通过 `useTaskUnderstandingState` 获取状态与动作
- `useTaskUnderstandingState.js` 内部消费 `commandDataModel.js` 的接口常量与 mock 构造
- `taskUnderstandingLocalDb.js` 负责本地数据库的初始化、读取与保存
- `ResourceCatalogPanel.vue` 读取本地数据库中的已解析资源集合，不再直接无条件展示全部静态资源

### 3.2 任务理解按钮（DECOMPOSE）
- 触发方法：`parseSelectedCommand`
- 位置：`useTaskUnderstandingState.js`
- 使用 URL：`COORDINATION_API_URLS.decompose`
- 位置：`useTaskUnderstandingState.js`

### 3.3 关联按钮（UPDATE: associate）
- 触发方法：`handleAssociate`
- 位置：`useTaskUnderstandingState.js`
- 使用 `buildUpdateRequest` 组包：`useTaskUnderstandingState.js`
- 使用 `buildUpdateResponse` 模拟响应：`useTaskUnderstandingState.js`
- 使用 URL：`COORDINATION_API_URLS.update`：`useTaskUnderstandingState.js`

### 3.4 删除按钮（UPDATE: delete）
- 触发方法：`removeSelectedCommand`
- 位置：`useTaskUnderstandingState.js`
- 使用 `buildUpdateRequest` 组包：`useTaskUnderstandingState.js`
- 使用 `buildUpdateResponse` 模拟响应：`useTaskUnderstandingState.js`
- 使用 URL：`COORDINATION_API_URLS.update`：`useTaskUnderstandingState.js`
- 删除确认弹窗与空态联动：`TaskUnderstandingPanel.vue`

### 3.5 依赖关系展示（dependencies）
- 任务条目显示依赖信息：模板位置 `TaskUnderstandingPanel.vue`
- 依赖格式化函数：`formatMissionDependencies`
- 位置：`useTaskUnderstandingState.js`

### 3.6 本地存储（前端本地数据库）
- 本地数据库实现：`localStorage + sessionStorage`
- 封装文件：`taskUnderstandingLocalDb.js`
- 当前存储内容：
  - `commands`
  - `analysisResultMap`
  - `selectedCommandId`
- 行为说明：
  - 同会话刷新：从本地库恢复
  - 新会话启动：以 mock 命令重建本地库
  - 页面启动后：任务理解与资源清单均从本地库读取

## 4. 影响与调整检查结论

已同步检查界面受影响点，结论如下：

1. 关联/删除按钮已按新 UPDATE 请求体/响应体结构对齐。
2. 任务理解流程已按新 DECOMPOSE URL 对齐。
3. `dependencies` 字段已从数据模型传递到界面并在任务结果中展示。
4. 当前仍为前端 mock 演示模式，尚未发起真实 HTTP 请求；后续接入真实后端时，仅需在现有构造函数基础上替换调用层。
5. 删除命令后，会同步清理本地数据库中的对应解析结果。
6. 资源清单当前以本地数据库中的“已解析资源集合”为准，与命令删除逻辑联动。

## 5. 建议后续接入步骤

1. 在 `useTaskUnderstandingState.js` 中将 `handleAssociate / handleDelete / parseSelectedCommand` 的 mock 响应替换为真实 `fetch/axios` 调用。
2. 保留 `commandDataModel.js` 作为请求响应模型层，避免在组件中散落字段定义。
3. 当后端字段调整时，优先改 `commandDataModel.js`，再按文档定位检查 `TaskUnderstandingPanel.vue / useTaskUnderstandingState.js` 使用点。
4. 当真实服务端接入后，可保留 `taskUnderstandingLocalDb.js` 作为缓存层，只替换初始化数据来源。

## 6. 相关文档

- `协同指控模块_前端代码架构说明.md`：组件分层、源码位置、后续子面板扩展方案

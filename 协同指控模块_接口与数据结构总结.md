# 协同指控模块：接口与数据结构总结

本文档用于说明本次“任务理解”相关接口定义与数据结构定义的代码位置，以及在界面中的使用情况。

## 1. 接口定义位置

接口常量与请求/响应构造函数统一定义在：
- `src/features/mission-control/modules/coordination/data/commandDataModel.js`

### 1.1 RESTful URL 定义
- `COORDINATION_API_URLS`（decompose / update）
- 位置：`commandDataModel.js:3`
- 当前值：
  - `decompose: /commandAndControl/command_decompose`
  - `update: /commandAndControl/command_update`

### 1.2 任务理解（DECOMPOSE）
- 请求体构造：`buildDecomposeRequest`
- 位置：`commandDataModel.js:274`
- 响应体构造：`buildDecomposeResponse`
- 位置：`commandDataModel.js:282`

### 1.3 关联/删除（UPDATE）
- 请求体构造：`buildUpdateRequest`
- 位置：`commandDataModel.js:294`
- 响应体构造：`buildUpdateResponse`
- 位置：`commandDataModel.js:314`

## 2. 数据结构定义位置

### 2.1 结构体（JSDoc Typedef）
定义均位于：`commandDataModel.js`

- `DependencyRefs`：`commandDataModel.js:9`
- `MissionDetail`：`commandDataModel.js:19`
- `MissionRecord`（包含 `dependencies` 字段）：`commandDataModel.js:29`
- `DecomposeRequest`：`commandDataModel.js:45`
- `UpdateRequest`：`commandDataModel.js:53`
- `DecomposeResponse`：`commandDataModel.js:69`
- `UpdateResponse`：`commandDataModel.js:83`

### 2.2 Mock 主数据
- 命令数据：`commandRecords`
- 任务数据：`missionRecords`
  - `dependencies` 字段示例位置：`commandDataModel.js:166`, `:191`, `:216`
- 资源数据：`resourceRecords`

### 2.3 按命令生成分析结果
- `createMockAnalysisByCommand`
- 位置：`commandDataModel.js:328`
- 该函数会把 URL、请求体、响应体、任务列表、资源列表拼装成前端直接可消费结构。

## 3. 界面使用位置

界面文件：
- `src/features/mission-control/modules/coordination/panels/CoordinationRightPanel.vue`

### 3.1 导入与初始化
- 导入接口与数据模型：`CoordinationRightPanel.vue:156-161`
- 使用 `createMockAnalysisByCommand` 初始化分析数据：`CoordinationRightPanel.vue:177`

### 3.2 任务理解按钮（DECOMPOSE）
- 触发方法：`parseSelectedCommand`
- 位置：`CoordinationRightPanel.vue:252`
- 使用 URL：`COORDINATION_API_URLS.decompose`
- 位置：`CoordinationRightPanel.vue:260`

### 3.3 关联按钮（UPDATE: associate）
- 触发方法：`handleAssociate`
- 位置：`CoordinationRightPanel.vue:211`
- 使用 `buildUpdateRequest` 组包：`CoordinationRightPanel.vue:217`
- 使用 `buildUpdateResponse` 模拟响应：`CoordinationRightPanel.vue:223`
- 使用 URL：`COORDINATION_API_URLS.update`：`CoordinationRightPanel.vue:228`

### 3.4 删除按钮（UPDATE: delete）
- 触发方法：`handleDelete`
- 位置：`CoordinationRightPanel.vue:233`
- 使用 `buildUpdateRequest` 组包：`CoordinationRightPanel.vue:238`
- 使用 `buildUpdateResponse` 模拟响应：`CoordinationRightPanel.vue:242`
- 使用 URL：`COORDINATION_API_URLS.update`：`CoordinationRightPanel.vue:247`

### 3.5 依赖关系展示（dependencies）
- 任务条目显示依赖信息：模板位置 `CoordinationRightPanel.vue:125`
- 依赖格式化函数：`formatMissionDependencies`
- 位置：`CoordinationRightPanel.vue:193`

## 4. 影响与调整检查结论

已同步检查界面受影响点，结论如下：

1. 关联/删除按钮已按新 UPDATE 请求体/响应体结构对齐。
2. 任务理解流程已按新 DECOMPOSE URL 对齐。
3. `dependencies` 字段已从数据模型传递到界面并在任务结果中展示。
4. 当前仍为前端 mock 演示模式，尚未发起真实 HTTP 请求；后续接入真实后端时，仅需在现有构造函数基础上替换调用层。

## 5. 建议后续接入步骤

1. 在 `CoordinationRightPanel.vue` 中将 `handleAssociate / handleDelete / parseSelectedCommand` 的 mock 响应替换为真实 `fetch/axios` 调用。
2. 保留 `commandDataModel.js` 作为请求响应模型层，避免在组件中散落字段定义。
3. 当后端字段调整时，优先改 `commandDataModel.js`，再按文档定位检查组件使用点。
